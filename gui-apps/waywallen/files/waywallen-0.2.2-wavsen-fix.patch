diff --git a/src/video/video_decoder.cpp b/src/video/video_decoder.cpp
index 5370f3b..8e58304 100644
--- a/src/video/video_decoder.cpp
+++ b/src/video/video_decoder.cpp
@@ -195,7 +195,7 @@ AVBufferRef* make_shared_vulkan_hwdevice(const Producer& vk, Error* err) {
 
     const auto& qfs = vk.queue_families();
     vctx->nb_qf = 0;
-    for (const auto& q : qfs) {
+    for (const auto q : qfs) {
         if (vctx->nb_qf >= static_cast<int>(sizeof(vctx->qf) / sizeof(vctx->qf[0])))
             break;
         AVVulkanDeviceQueueFamily entry {};
diff --git a/src/video/vk_device.cpp b/src/video/vk_device.cpp
index dc339b4..d04f0be 100644
--- a/src/video/vk_device.cpp
+++ b/src/video/vk_device.cpp
@@ -37,7 +37,7 @@ bool device_has_ext(VkPhysicalDevice phys, const char* name) {
     vkEnumerateDeviceExtensionProperties(phys, nullptr, &n, nullptr);
     std::vector<VkExtensionProperties> props(n);
     vkEnumerateDeviceExtensionProperties(phys, nullptr, &n, props.data());
-    for (auto& p : props) {
+    for (auto p : props) {
         if (std::strcmp(p.extensionName, name) == 0) return true;
     }
     return false;
@@ -186,7 +186,7 @@ Producer::build_(uint32_t width, uint32_t height,
                                   "vkGetPhysicalDeviceProperties2"));
 
     bool have_drm_ext = false;
-    for (auto pd : pds) {
+    for (auto& pd : pds) {
         bool ok = true;
         for (const char* e : req_dev_exts) {
             if (!device_has_ext(pd, e)) { ok = false; break; }
@@ -260,7 +260,7 @@ Producer::build_(uint32_t width, uint32_t height,
     std::vector<float>                   prios(self->queue_families_.size(), 1.0f);
     std::vector<VkDeviceQueueCreateInfo> qcis;
     qcis.reserve(self->queue_families_.size());
-    for (const auto& q : self->queue_families_) {
+    for (const auto q : self->queue_families_) {
         VkDeviceQueueCreateInfo qci {};
         qci.sType            = VK_STRUCTURE_TYPE_DEVICE_QUEUE_CREATE_INFO;
         qci.queueFamilyIndex = q.index;
